// Placeholder smoke test. The real app entry depends on async env loading
// and Supabase initialisation, which need a fuller test harness.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
